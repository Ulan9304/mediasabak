from django.conf.urls import url
from django.contrib import admin
from app_views import views

urlpatterns = [
    # url(r'^$', name='lesson'),
    # url = (r'^$',views.LessonDetail.as_view(),name = "detail"),
]